package infrastructure_test

import (
	"testing"
)

func TestDomain(t *testing.T) {
	// testdata := filepath.Join(analysistest.TestData(), "clean-layer")
	// results := analysistest.Run(t, testdata, infrastructure.Analyzer, "domain")
	// for _, r := range results {
	// 	for _, d := range r.Diagnostics {
	// 		log.Print(d.Message)
	// 	}
	// }
}

func TestUsecase(t *testing.T) {
	// testdata := filepath.Join(analysistest.TestData(), "clean-layer")
	// results := analysistest.Run(t, testdata, infrastructure.Analyzer, "usecase")
	// for _, r := range results {
	// 	for _, d := range r.Diagnostics {
	// 		log.Print(d.Message)
	// 	}
	// }
}
